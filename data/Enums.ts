enum RoomType {
    All,
    Room1,
    Room2
}

enum GusestState {
    wait,
    request,
    checkin,
    checkout
}

enum PriceType {
    All,
    week,
    month
}

enum AxiosResponse {
    Sucessful = 200
}

enum Sex {
    none,
    female,
    male
}


export {
    RoomType,
    PriceType,
    GusestState,
    AxiosResponse,
    Sex
}

